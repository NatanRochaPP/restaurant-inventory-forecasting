[pytest]
testpaths = tests
addopts = --strict-markers
markers =
    slow: end-to-end tests over the full 14,620-row dataset (deselect with '-m "not slow"')
filterwarnings =
    ignore::DeprecationWarning
